[project]
name = "symbiosis"
version = "0.1.0"
description = "An open-source AI gateway"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "ruff>=0.14.4",
]
